SELECT
    transaction_id,
    event_timestamp,
    cc_num_hash,
    merchant,
    category,
    amt,
    customer_first_name,
    customer_last_name,
    gender,
    state,
    city,
    zip,
    job,
    age_group,
    is_fraud,
    risk_score,
    is_blacklisted,
    compliance_status,
    fraud_flag,
    risk_score_computed,
    high_amount_flag,
    high_velocity_flag,
    high_risk_merchant_flag,
    blacklisted_merchant_flag
FROM {{ source('silver', 'transactions_enriched') }}